{
  "name": "@refpool/benchmark-eviction-throughput",
  "version": "0.0.0",
  "private": true,
  "description": "Eviction-throughput benchmark: proves the acquire/evict hot path is O(1) by holding per-op cost flat as the pool's `max` (and working set) scale from 64 to 32k.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "bench": "node --import tsx src/index.ts"
  },
  "dependencies": {
    "@refpool/core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
